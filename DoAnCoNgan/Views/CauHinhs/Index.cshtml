@model IEnumerable<DoAnCoNgan.Models.CauHinh>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCauHinh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIATRI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCauHinh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GIATRI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCauHinh }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCauHinh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCauHinh })
        </td>
    </tr>
}

</table>
